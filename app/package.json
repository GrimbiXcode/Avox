{
  "name": "avox-app",
  "version": "0.0.1",
  "description": "Avox — GUI (Tauri v2)",
  "private": true,
  "license": "GPL-2.0-only",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
